<Page
    x:Class="Arendi.HubPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Arendi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    d:DataContext="{Binding Source={d:DesignData Source=../Arendi.Shared/DataModel/SampleData.json, Type=data:SampleDataSource}}"
    xmlns:data="using:Arendi.Data"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar IsSticky="True" x:Name="appBar" Background="#FF950E">
            <CommandBar.PrimaryCommands>
                <!--<AppBarButton Icon="ZoomOut" IsCompact="False" Label="ZoomOut"/>-->
                <!--<AppBarButton Icon="ZoomIn" IsCompact="False" Label="ZoomIn"/> -->
            </CommandBar.PrimaryCommands>
            <CommandBar.SecondaryCommands>
                <AppBarButton Label="Ayarlar"/>
                <AppBarButton Label="Yardım"/>
                <AppBarButton x:ConnectionId='1' Label="Çıkış"                            />
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Default">
                    <ImageBrush x:Key="HubBackgroundImageBrush" ImageSource="../Assets/HubBackground.theme-light.png"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <ImageBrush x:Key="HubBackgroundImageBrush" ImageSource="{x:Null}"/>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
            
            <!-- For collection binding -->
            <CollectionViewSource x:Name="IdeaCollection"/>

            <DataTemplate x:Key="HubSectionHeaderTemplate">
                <TextBlock Margin="0,0,0,-9.5" Text="{Binding}"/>
            </DataTemplate>

            <!-- Grid-appropriate item template as seen in section 2 -->
            <DataTemplate x:Key="Standard200x180TileItemTemplate">
                <Grid Margin="0,0,9.5,9.5" Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}">
                    <Image Source="{Binding ImagePath}" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}" Height="138.5" Width="138.5"/>
                    <TextBlock Text="{Binding Title}" VerticalAlignment="Bottom" Margin="9.5,0,0,6.5" Style="{ThemeResource BaseTextBlockStyle}"/>
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="IdeaTemplate">

                <Grid>
                    <Border BorderBrush="White"  BorderThickness="5" CornerRadius="12" Width="350" Margin="10,10,10,10" Background="Transparent">
                        <Grid>
                            <!-- Username: iUsername -->
                            <TextBlock FontFamily="/Fonts/gothic_0.TTF#Century Gothic" VerticalAlignment="Top" TextWrapping="Wrap" Text="{Binding Path=iUsername}" Height="{Binding}" FontSize="20" Foreground="White" Margin="17,10,30,5"/>

                            <!-- Ruler -->
                            <Border Margin="17,40,30,5" Height="2" VerticalAlignment="Top" BorderBrush="White" BorderThickness="2" />

                            <!-- Idea header: iHeader -->
                            <TextBlock FontFamily="/Fonts/gothic_0.TTF#Century Gothic" VerticalAlignment="Top" TextWrapping="Wrap" Text="{Binding Path=iHeader}" Height="{Binding}" FontSize="20" Foreground="White" Margin="17,50,30,5"/>

                            <!-- Idea content: iContent -->
                            <TextBlock FontFamily="/Fonts/gothic_0.TTF#Century Gothic" VerticalAlignment="Top" TextWrapping="Wrap" Text="{Binding Path=iContent}" Height="{Binding}" FontSize="20" Foreground="White" Margin="17,80,30,5"/>

                            <Button Content="Beğen" FontFamily="/Fonts/gothic_0.TTF#Century Gothic" Style="{StaticResource Custom_Button}" Margin="17,115,0,0" Width="145"/>
                            <Button Content="Yorum" FontFamily="/Fonts/gothic_0.TTF#Century Gothic" Style="{StaticResource Custom_Button}" Margin="184,115,12,0" Width="145"/>
                        </Grid>
                    </Border>
                </Grid>

            </DataTemplate>

            <DataTemplate x:Key="StandardTripleLineItemTemplate">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}" Margin="0,9.5,0,0" Grid.Column="0" HorizontalAlignment="Left">
                        <Image Source="{Binding ImagePath}" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}" Height="79" Width="79"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="14.5,0,0,0">
                        <TextBlock Text="{Binding Title}" Style="{ThemeResource ListViewItemTextBlockStyle}"/>
                        <TextBlock Text="{Binding Description}" Style="{ThemeResource ListViewItemContentTextBlockStyle}" Foreground="{ThemeResource PhoneMidBrush}" />
                        <TextBlock Text="{Binding Subtitle}" Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}" />
                    </StackPanel>
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="StandardDoubleLineItemTemplate">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}" Margin="0,9.5,0,0" Grid.Column="0" HorizontalAlignment="Left">
                        <Image Source="{Binding ImagePath}" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}" Height="79" Width="79"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="14.5,0,0,0">
                        <TextBlock Text="{Binding Title}" Style="{ThemeResource ListViewItemTextBlockStyle}"/>
                        <TextBlock Text="{Binding Subtitle}" Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}"/>
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </Page.Resources>


    <Grid x:Name="LayoutRoot">
        <Hub x:Name="Hub" Header="FİKİR DEPOSU" Background="{ThemeResource HubBackgroundImageBrush}" Style="{StaticResource Custom_Hub}">
            
            <HubSection Margin="0,5,0,0" Header="ana sayfa" DataContext="{Binding Groups}" Style="{StaticResource Custom_HubSection}">
                <DataTemplate>
                    <ListView HorizontalAlignment="Center" 
                              VerticalAlignment="Bottom"
                              ItemsSource="{Binding Source={StaticResource IdeaCollection}}"
                              Height="500" 
                              Margin="5,5,5,5" 
                              Width="380"
                              ItemTemplate="{StaticResource IdeaTemplate}" />
                </DataTemplate>
            </HubSection>

            <HubSection Margin="0,5,0,0"  Header="fikirlerim" Width="Auto"
                         DataContext="{Binding Groups[0]}" Style="{StaticResource Custom_HubSection}">
                <!--
                <DataTemplate>
                    <GridView
                        Margin="0,9.5,0,0"
                        ItemsSource="{Binding Items}"
                        AutomationProperties.AutomationId="ItemGridView"
                        AutomationProperties.Name="Items In Group"
                        ItemTemplate="{StaticResource Standard200x180TileItemTemplate}"
                        SelectionMode="None"
                        IsItemClickEnabled="True"
                        ItemClick="ItemView_ItemClick"
                        ContinuumNavigationTransitionInfo.ExitElementContainer="True">
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                    </GridView>
                </DataTemplate>
                -->
            </HubSection>

            <HubSection Margin="0,5,0,0"  Header="beğendiklerim"
                        DataContext="{Binding Groups[1]}" Style="{StaticResource Custom_HubSection}">
                <!--
                <DataTemplate>
                    <ListView 
                        AutomationProperties.AutomationId="ItemListViewSection3"
                        AutomationProperties.Name="Items In Group"
                        SelectionMode="None"
                        IsItemClickEnabled="True"
                        ItemsSource="{Binding Items}"
                        ItemTemplate="{StaticResource StandardTripleLineItemTemplate}"
                        ItemClick="ItemView_ItemClick"
                        ContinuumNavigationTransitionInfo.ExitElementContainer="True">
                    </ListView>
                </DataTemplate>
                -->
            </HubSection>

        </Hub>
    </Grid>
</Page>

